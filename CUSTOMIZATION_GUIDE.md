# Руководство по безопасной кастомизации бота

## 🎭 1. Изменить ЛИЧНОСТЬ бота (как она говорит)

**Файл:** `ai/prompts.py`

**Что делать:**
1. Открыть `ai/prompts.py`
2. Найти `SYSTEM_PROMPT` (строка ~10)
3. Отредактировать только текст промпта
4. Перезапустить бота: `Ctrl+C` → `python main.py`

**Пример изменений:**
- Сделать более формальной/неформальной
- Добавить новые инструкции по общению
- Изменить тон (дружелюбный/профессиональный)

**НЕ НУЖНО:**
- ❌ Пересобирать ChromaDB
- ❌ Менять код в других файлах
- ❌ Создавать новые файлы

---

## 📖 2. Добавить ЗНАНИЯ (товары, скрипты продаж)

**Папка:** `data/knowledge_base/`

### Добавить описание товаров:

1. **Открыть:** `data/knowledge_base/catalog/Каталог товаров.docx`
2. **Добавить** новый раздел с описанием товара (по аналогии)
3. **Пересобрать базу:**
   ```bash
   python -m knowledge.builder
   ```
4. **Перезапустить бота**

### Добавить скрипт продаж:

1. **Открыть:** `data/knowledge_base/scripts/*.docx`
2. **Отредактировать** или добавить новый файл
3. **Пересобрать базу:**
   ```bash
   python -m knowledge.builder
   ```
4. **Перезапустить бота**

### Добавить примеры диалогов:

1. **Создать папку:** `data/knowledge_base/chats/новая_папка/`
2. **Экспортировать чат** из WhatsApp (без медиа, только текст)
3. **Положить файл:** `_chatN.txt` в папку
4. **Пересобрать базу**

**НЕ НУЖНО:**
- ❌ Менять код
- ❌ Трогать другие файлы

---

## 🖼️ 3. Добавить ФОТО товаров

**Где:** Google Drive папка (ID: `11jDqeLwvLL5VJKzoGn9-ZlDUBBNSc5e_`)

### Как добавить:

1. **Откройте Google Drive** → найдите папку с фото
2. **Загрузите новое фото** с правильным именем:
   - Формат: `Бренд Название товара цвет.jpg`
   - Примеры:
     - `Chanel Classic Slingbacks черные.jpg`
     - `Jimmy Choo Azia 95 бежевые.jpg`
     - `Golden Goose Super-Star белые.jpg`

3. **Перезапустить бота** (он пересоберёт индекс автоматически)

**Организация по папкам (опционально):**
- Можно создать подпапки: `Обувь/`, `Сумки/`, `Аксессуары/`
- Бот автоматически сканирует все подпапки

**НЕ НУЖНО:**
- ❌ Менять код
- ❌ Редактировать JSON файлы

---

## 📊 4. Обновить НАЛИЧИЕ товаров (inventory)

**Файл:** `data/inventory.xlsx`

### Как обновить:

1. **Открыть:** `data/inventory.xlsx` в Excel
2. **Изменить** данные:
   - `product_name` - название товара
   - `size` - размер (для обуви)
   - `color` - цвет
   - `quantity` - количество (0 = нет в наличии)
   - `price` - цена в тенге
3. **Сохранить файл**
4. **Подождать 5 минут** (автообновление кеша) ИЛИ перезапустить бота

**НЕ НУЖНО:**
- ❌ Пересобирать ChromaDB
- ❌ Менять код

---

## ⚙️ 5. Изменить ЛОГИКУ ЗАКАЗА

**Файлы:**
- `ai/order_manager.py` - чистая бизнес-логика (без I/O)
- `ai/engine.py` - основной оркестратор

### Безопасные изменения в `ai/order_manager.py`:

**Что можно менять:**
- Какие поля запрашивать (city, size, color, address)
- Порядок вопросов
- Валидация данных
- Текст подтверждения заказа

**Как делать:**
1. Открыть `ai/order_manager.py`
2. Найти нужную функцию (например `_build_missing_fields`)
3. Изменить логику
4. **Запустить тесты:**
   ```bash
   pytest tests/test_order_manager.py -v
   ```
5. Если тесты прошли → перезапустить бота

**НЕ НУЖНО:**
- ❌ Менять сигнатуры функций (имена, параметры)
- ❌ Удалять используемые функции
- ❌ Менять ai/engine.py без понимания потока

---

## 🔧 6. Изменить НАСТРОЙКИ (без кода)

**Файл:** `.env`

### Что можно менять:

```bash
# Поведение бота
MAX_CONVERSATION_HISTORY=20        # История диалога (20-50)
MAX_RAG_RESULTS=5                  # Результатов из базы знаний (3-10)
MAX_PHOTOS_PER_MESSAGE=3           # Фото за раз (1-10)

# Nudge (напоминания клиентам)
NUDGE_ENABLED=1                    # 1=вкл, 0=выкл
NUDGE_CHECK_INTERVAL_MINUTES=5     # Как часто проверять (5-30)

# OpenAI модель
OPENAI_MODEL=gpt-4o                # gpt-4o / gpt-4-turbo / gpt-4
```

**После изменения:**
- Перезапустить бота

---

## 🧪 7. ТЕСТИРОВАНИЕ изменений (обязательно!)

### Перед деплоем:

```bash
# 1. Запустить все тесты
pytest tests/ -v

# 2. Проверить импорты
python -c "from main import app; print('OK')"

# 3. Проверить базу знаний
python -m knowledge.builder

# 4. Локальный запуск
python main.py
# Отправить тестовые сообщения в WhatsApp
```

---

## ✅ Чек-лист безопасных изменений

### Для каждого изменения:

- [ ] Определил **ТИП изменения** (промпт/знания/фото/inventory/код)
- [ ] Изменил **ТОЛЬКО нужные файлы** по инструкции выше
- [ ] Запустил **тесты** (если менял код)
- [ ] **Перезапустил бота** локально
- [ ] **Протестировал** изменения в WhatsApp
- [ ] **Сделал git commit** с понятным описанием
- [ ] **Только потом** деплоил на сервер

---

## 🚨 Что НЕЛЬЗЯ делать

### ❌ Антипаттерны:

1. **Менять код без тестов**
   - Всегда запускайте `pytest tests/ -v` после изменений

2. **Создавать дубликаты файлов**
   - Не делайте `ai/engine_v2.py` или `prompts_old.py`
   - Используйте git для версионирования

3. **Менять несколько типов одновременно**
   - Плохо: промпт + код + база знаний + inventory за раз
   - Хорошо: одно изменение → тест → commit → следующее

4. **Игнорировать git**
   - Делайте `git commit` после каждого успешного изменения
   - Так можно откатиться если что-то сломалось

5. **Менять структуру БД напрямую**
   - Не редактируйте `data/ottenok.db` руками
   - Используйте только через код

---

## 📝 Примеры частых задач

### "Хочу, чтобы бот был более дружелюбным"
→ Редактируем `ai/prompts.py`, строка ~10

### "Добавить новый товар Jimmy Choo"
→ 1) Добавляем в `data/knowledge_base/catalog/Каталог товаров.docx`
→ 2) Загружаем фото в Google Drive
→ 3) Добавляем в `data/inventory.xlsx`
→ 4) `python -m knowledge.builder`

### "Изменить вопрос про город"
→ Редактируем `ai/order_manager.py`, функция `_question_for_missing`

### "Бот не должен спрашивать размер для сумок"
→ Уже работает! Проверьте `ai/order_manager.py:_SIZE_REQUIRED_TYPES`

---

## 🔄 Workflow для production

```bash
# 1. Локально делаем изменения
vim ai/prompts.py

# 2. Тестируем
pytest tests/ -v
python main.py  # отправляем тестовые сообщения

# 3. Коммитим
git add ai/prompts.py
git commit -m "Update bot persona: make more friendly"

# 4. Пушим на сервер
git push origin master

# 5. На сервере
ssh user@server
cd /opt/sales_ottenok
git pull
sudo systemctl restart sales_ottenok  # или docker compose restart
```

---

## 📚 Что дальше?

Скажите мне **конкретно что хотите изменить**, и я помогу:
1. Найти правильный файл
2. Показать как безопасно изменить
3. Проверить что ничего не сломалось
